class ButtonModel {
    constructor(buttonNameId, buttonActiveId, buttonInactiveId) {
        this.buttonId = buttonNameId,
        this.active = buttonActiveId,
        this.inactive = buttonInactiveId,
        this.state = false
    };
}